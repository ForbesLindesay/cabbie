{
  "name": "cabbie-sync-test",
  "private": true,
  "dependencies": {
    "cabbie-sync": "../../source/tarballs/cabbie-sync-0.12.1.tgz",
    "chromedriver": "^2.46.0",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.4000"
  },
  "scripts": {
    "setup": "yarn add ../../source/tarballs/cabbie-sync-*.tgz",
    "test": "ts-node src",
    "test:record": "yarn test -- chromedriver --record ../snapshot.json",
    "test:replay": "yarn test -- chromedriver --replay ../snapshot.json"
  }
}
